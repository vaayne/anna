version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gocritic
    - misspell

formatters:
  enable:
    - gofmt

issues:
  exclude-dirs:
    - bin
    - vendor
